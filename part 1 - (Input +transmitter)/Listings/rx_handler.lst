C51 COMPILER V9.60.7.0   RX_HANDLER                                                        12/29/2025 17:52:14 PAGE 1   


C51 COMPILER V9.60.7.0, COMPILATION OF MODULE RX_HANDLER
OBJECT MODULE PLACED IN .\Objects\rx_handler.obj
COMPILER INVOKED BY: C:\Keil_v5\C51\BIN\C51.EXE rx_handler.c OPTIMIZE(8,SPEED) BROWSE DEBUG OBJECTEXTEND PRINT(.\Listing
                    -s\rx_handler.lst) TABS(2) OBJECT(.\Objects\rx_handler.obj)

line level    source

   1          /* File: rx_handler.c */
   2          /*
   3           * Function: rx_handler
   4           * --------------------
   5           * Processes a single character received from UART.
   6           * 1. Converts ASCII Hex character ('0'-'9', 'A'-'F') to integer value.
   7           * 2. Expands the 4-bit nibble into individual bits in the S_stream_expanded array.
   8           * 3. Updates the buffer count and sets a flag for the main loop.
   9           */
  10          #include <aduc841.h>
  11          #include "header.h"
  12          
  13          void rx_handler(uint8_t rx_char)
  14          {
  15   1          uint8_t parsed_val = 0xFF; // Initialize as invalid (0xFF)
  16   1          uint8_t i;
  17   1          uint8_t index;
  18   1        
  19   1          if (rx_char == '\r' || rx_char == '\n')
  20   1          {
  21   2            buffer_flag = 1;       
  22   2            return; 
  23   2          }
  24   1      
  25   1          // --- 1. ASCII to Hex Conversion ---
  26   1          
  27   1          // Check if character is a digit '0' through '9'
  28   1          if (rx_char >= '0' && rx_char <= '9')       
  29   1          {
  30   2              parsed_val = rx_char - '0';
  31   2          }
  32   1          // Check if character is a letter 'A' through 'F'
  33   1          else if (rx_char >= 'A' && rx_char <= 'F') 
  34   1          {
  35   2              parsed_val = rx_char - 'A' + 10;
  36   2          }
  37   1      
  38   1          // --- 2. Process Valid Data ---
  39   1          
  40   1          // Proceed only if the character was a valid Hex digit
  41   1          if (parsed_val != 0xFF)
  42   1          {
  43   2              Snew = parsed_val; // Store the raw syndrome value
  44   2      
  45   2              // Check if there is space in the buffer
  46   2              if (buffer_count < MAX_PACKETS)
  47   2              {
  48   3                  // --- 3. Bitwise Expansion ---
  49   3                  // Loop to extract each bit from the nibble (MSB to LSB)
  50   3                  // and store it as a separate byte in the expanded array.
  51   3                  for (i = 0; i < HAMMING_R; i++)
  52   3                  {
  53   4                      // Calculate the linear index in the global xdata array
  54   4                      index = (buffer_count * HAMMING_R) + i;
C51 COMPILER V9.60.7.0   RX_HANDLER                                                        12/29/2025 17:52:14 PAGE 2   

  55   4                      
  56   4                      // Logic:
  57   4                      // 1. Shift 'parsed_val' right to move the target bit to position 0.
  58   4                      // 2. Mask with 0x01 to isolate that single bit.
  59   4                      S_stream_expanded[index] = 
  60   4                          (parsed_val >> (HAMMING_R - 1 - i)) & 0x01;
  61   4                  }
  62   3                  
  63   3                  buffer_count++; // Increment the number of received packets
  64   3                  
  65   3                  // --- 4. Signal Main Loop ---
  66   3                  // Raise the flag to notify main() that data is ready for decoding
  67   3              }
  68   2          }
  69   1      }


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    106    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =   ----    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
